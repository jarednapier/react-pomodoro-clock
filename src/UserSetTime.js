import React from 'react';
import './CSS/UserSetTime.css';
import PropTypes from 'prop-types';

const UserSetTime = (props) => {
    return (
        <p>UserSetTime here</p>
    );
};

UserSetTime.propTypes = {

};

export default UserSetTime;