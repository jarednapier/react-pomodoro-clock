import React from 'react';
import './CSS/Timer.css';
import PropTypes from 'prop-types';

const Timer = (props) => {
    return (
        <p>Timer here</p>
    );
};

Timer.propTypes = {

};

export default Timer;